/* eslint-disable */
/**
 * This file was automatically generated by @algorandfoundation/algokit-client-generator.
 * DO NOT MODIFY IT BY HAND.
 * requires: @algorandfoundation/algokit-utils: ^7
 */
import { type AlgorandClient } from '@algorandfoundation/algokit-utils/types/algorand-client'
import { ABIReturn, AppReturn, SendAppTransactionResult } from '@algorandfoundation/algokit-utils/types/app'
import { Arc56Contract, getArc56ReturnValue, getABIStructFromABITuple } from '@algorandfoundation/algokit-utils/types/app-arc56'
import {
  AppClient as _AppClient,
  AppClientMethodCallParams,
  AppClientParams,
  AppClientBareCallParams,
  CallOnComplete,
  AppClientCompilationParams,
  ResolveAppClientByCreatorAndName,
  ResolveAppClientByNetwork,
  CloneAppClientParams,
} from '@algorandfoundation/algokit-utils/types/app-client'
import { AppFactory as _AppFactory, AppFactoryAppClientParams, AppFactoryResolveAppClientByCreatorAndNameParams, AppFactoryDeployParams, AppFactoryParams, CreateSchema } from '@algorandfoundation/algokit-utils/types/app-factory'
import { TransactionComposer, AppCallMethodCall, AppMethodCallTransactionArgument, SimulateOptions, RawSimulateOptions, SkipSignaturesSimulateOptions } from '@algorandfoundation/algokit-utils/types/composer'
import { SendParams, SendSingleTransactionResult, SendAtomicTransactionComposerResults } from '@algorandfoundation/algokit-utils/types/transaction'
import { Address, encodeAddress, modelsv2, OnApplicationComplete, Transaction, TransactionSigner } from 'algosdk'
import SimulateResponse = modelsv2.SimulateResponse

export const APP_SPEC: Arc56Contract = {"name":"PassportRegistry","structs":{},"methods":[{"name":"register_passport","args":[{"type":"string","name":"passport_id","desc":"Unique identifier for the MSME passport"},{"type":"byte[]","name":"passport_hash","desc":"SHA-256 hash of the passport JSON"}],"returns":{"type":"void"},"actions":{"create":[],"call":["NoOp"]},"readonly":false,"desc":"Register a passport hash on-chain.","events":[],"recommendations":{}},{"name":"verify_passport","args":[{"type":"string","name":"passport_id","desc":"Unique identifier for the MSME passport"}],"returns":{"type":"byte[]","desc":"The SHA-256 hash stored on-chain"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Retrieve the stored hash for a passport (read-only).","events":[],"recommendations":{}},{"name":"passport_exists","args":[{"type":"string","name":"passport_id"}],"returns":{"type":"bool"},"actions":{"create":[],"call":["NoOp"]},"readonly":true,"desc":"Check if a passport is registered.","events":[],"recommendations":{}}],"arcs":[22,28],"desc":"\n    Stores and verifies MSME Credit Reputation Passport hashes on-chain.\n    Uses BoxMap for scalable, per-passport storage.\n    ","networks":{},"state":{"schema":{"global":{"ints":0,"bytes":0},"local":{"ints":0,"bytes":0}},"keys":{"global":{},"local":{},"box":{}},"maps":{"global":{},"local":{},"box":{"passport_hashes":{"keyType":"AVMString","valueType":"AVMBytes","prefix":"cGg="}}}},"bareActions":{"create":["NoOp"],"call":[]},"sourceInfo":{"approval":{"sourceInfo":[{"pc":[102],"errorMessage":"Passport ID cannot be empty"},{"pc":[134],"errorMessage":"Passport not found"},{"pc":[70,86,119,154],"errorMessage":"invalid array length header"},{"pc":[77,93,126,161],"errorMessage":"invalid number of bytes for arc4.dynamic_array<arc4.uint8>"}],"pcOffsetMethod":"none"},"clear":{"sourceInfo":[],"pcOffsetMethod":"none"}},"source":{"approval":"","clear":"I3ByYWdtYSB2ZXJzaW9uIDExCiNwcmFnbWEgdHlwZXRyYWNrIGZhbHNlCgovLyBhbGdvcHkuYXJjNC5BUkM0Q29udHJhY3QuY2xlYXJfc3RhdGVfcHJvZ3JhbSgpIC0+IHVpbnQ2NDoKbWFpbjoKICAgIHB1c2hpbnQgMQogICAgcmV0dXJuCg=="},"byteCode":{"approval":"CyADAAIBJgICcGgEFR98dTEbQQAkMRkURDEYRIIDBIk1lJgEBbWBVAQ4g5QZNhoAjgMACQA6AF0AMRkUMRgUEEM2GgFJIlkjCEsBFRJEVwIANhoCSSJZIwhLARUSRFcCAEsBgAATRChPAlBJvEhMvyRDNhoBSSJZIwhLARUSRFcCAChMUL5ESRUWVwYCTFApTFCwJEM2GgFJIlkjCEsBFRJEVwIAKExQvUUBgAEAIk8CVClMULAkQw==","clear":"C4EBQw=="},"compilerInfo":{"compiler":"puya","compilerVersion":{"major":5,"minor":8,"patch":1}},"events":[],"templateVariables":{}} as unknown as Arc56Contract

/**
 * A state record containing binary data
 */
export interface BinaryState {
  /**
   * Gets the state value as a Uint8Array
   */
  asByteArray(): Uint8Array | undefined
  /**
   * Gets the state value as a string
   */
  asString(): string | undefined
}

class BinaryStateValue implements BinaryState {
  constructor(private value: Uint8Array | undefined) {}

  asByteArray(): Uint8Array | undefined {
    return this.value
  }

  asString(): string | undefined {
    return this.value !== undefined ? Buffer.from(this.value).toString('utf-8') : undefined
  }
}

/**
 * Expands types for IntelliSense so they are more human readable
 * See https://stackoverflow.com/a/69288824
 */
export type Expand<T> = T extends (...args: infer A) => infer R
  ? (...args: Expand<A>) => Expand<R>
  : T extends infer O
    ? { [K in keyof O]: O[K] }
    : never


/**
 * The argument types for the PassportRegistry contract
 */
export type PassportRegistryArgs = {
  /**
   * The object representation of the arguments for each method
   */
  obj: {
    'register_passport(string,byte[])void': {
      /**
       * Unique identifier for the MSME passport
       */
      passportId: string
      /**
       * SHA-256 hash of the passport JSON
       */
      passportHash: Uint8Array
    }
    'verify_passport(string)byte[]': {
      /**
       * Unique identifier for the MSME passport
       */
      passportId: string
    }
    'passport_exists(string)bool': {
      passportId: string
    }
  }
  /**
   * The tuple representation of the arguments for each method
   */
  tuple: {
    'register_passport(string,byte[])void': [passportId: string, passportHash: Uint8Array]
    'verify_passport(string)byte[]': [passportId: string]
    'passport_exists(string)bool': [passportId: string]
  }
}

/**
 * The return type for each method
 */
export type PassportRegistryReturns = {
  'register_passport(string,byte[])void': void
  'verify_passport(string)byte[]': Uint8Array
  'passport_exists(string)bool': boolean
}

/**
 * Defines the types of available calls and state of the PassportRegistry smart contract.
 */
export type PassportRegistryTypes = {
  /**
   * Maps method signatures / names to their argument and return types.
   */
  methods:
    & Record<'register_passport(string,byte[])void' | 'register_passport', {
      argsObj: PassportRegistryArgs['obj']['register_passport(string,byte[])void']
      argsTuple: PassportRegistryArgs['tuple']['register_passport(string,byte[])void']
      returns: PassportRegistryReturns['register_passport(string,byte[])void']
    }>
    & Record<'verify_passport(string)byte[]' | 'verify_passport', {
      argsObj: PassportRegistryArgs['obj']['verify_passport(string)byte[]']
      argsTuple: PassportRegistryArgs['tuple']['verify_passport(string)byte[]']
      /**
       * The SHA-256 hash stored on-chain
       */
      returns: PassportRegistryReturns['verify_passport(string)byte[]']
    }>
    & Record<'passport_exists(string)bool' | 'passport_exists', {
      argsObj: PassportRegistryArgs['obj']['passport_exists(string)bool']
      argsTuple: PassportRegistryArgs['tuple']['passport_exists(string)bool']
      returns: PassportRegistryReturns['passport_exists(string)bool']
    }>
  /**
   * Defines the shape of the state of the application.
   */
  state: {
    box: {
      keys: {}
      maps: {
        passportHashes: Map<string, Uint8Array>
      }
    }
  }
}

/**
 * Defines the possible abi call signatures.
 */
export type PassportRegistrySignatures = keyof PassportRegistryTypes['methods']
/**
 * Defines the possible abi call signatures for methods that return a non-void value.
 */
export type PassportRegistryNonVoidMethodSignatures = keyof PassportRegistryTypes['methods'] extends infer T ? T extends keyof PassportRegistryTypes['methods'] ? MethodReturn<T> extends void ? never : T  : never : never
/**
 * Defines an object containing all relevant parameters for a single call to the contract.
 */
export type CallParams<TArgs> = Expand<
  Omit<AppClientMethodCallParams, 'method' | 'args' | 'onComplete'> &
    {
      /** The args for the ABI method call, either as an ordered array or an object */
      args: Expand<TArgs>
    }
>
/**
 * Maps a method signature from the PassportRegistry smart contract to the method's arguments in either tuple or struct form
 */
export type MethodArgs<TSignature extends PassportRegistrySignatures> = PassportRegistryTypes['methods'][TSignature]['argsObj' | 'argsTuple']
/**
 * Maps a method signature from the PassportRegistry smart contract to the method's return type
 */
export type MethodReturn<TSignature extends PassportRegistrySignatures> = PassportRegistryTypes['methods'][TSignature]['returns']

/**
 * Defines the shape of the keyed box state of the application.
 */
export type BoxKeysState = PassportRegistryTypes['state']['box']['keys']


/**
 * Defines supported create method params for this smart contract
 */
export type PassportRegistryCreateCallParams =
  | Expand<AppClientBareCallParams & {method?: never} & {onComplete?: OnApplicationComplete.NoOpOC} & CreateSchema>
/**
 * Defines arguments required for the deploy method.
 */
export type PassportRegistryDeployParams = Expand<Omit<AppFactoryDeployParams, 'createParams' | 'updateParams' | 'deleteParams'> & {
  /**
   * Create transaction parameters to use if a create needs to be issued as part of deployment; use `method` to define ABI call (if available) or leave out for a bare call (if available)
   */
  createParams?: PassportRegistryCreateCallParams
}>


/**
 * Exposes methods for constructing `AppClient` params objects for ABI calls to the PassportRegistry smart contract
 */
export abstract class PassportRegistryParamsFactory {
  /**
   * Constructs a no op call for the register_passport(string,byte[])void ABI method
   *
   * Register a passport hash on-chain.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static registerPassport(params: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'register_passport(string,byte[])void' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.passportId, params.args.passportHash],
    }
  }
  /**
   * Constructs a no op call for the verify_passport(string)byte[] ABI method
   *
   * Retrieve the stored hash for a passport (read-only).
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static verifyPassport(params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'verify_passport(string)byte[]' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.passportId],
    }
  }
  /**
   * Constructs a no op call for the passport_exists(string)bool ABI method
   *
   * Check if a passport is registered.
   *
   * @param params Parameters for the call
   * @returns An `AppClientMethodCallParams` object for the call
   */
  static passportExists(params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']> & CallOnComplete): AppClientMethodCallParams & CallOnComplete {
    return {
      ...params,
      method: 'passport_exists(string)bool' as const,
      args: Array.isArray(params.args) ? params.args : [params.args.passportId],
    }
  }
}

/**
 * A factory to create and deploy one or more instance of the PassportRegistry smart contract and to create one or more app clients to interact with those (or other) app instances
 */
export class PassportRegistryFactory {
  /**
   * The underlying `AppFactory` for when you want to have more flexibility
   */
  public readonly appFactory: _AppFactory

  /**
   * Creates a new instance of `PassportRegistryFactory`
   *
   * @param params The parameters to initialise the app factory with
   */
  constructor(params: Omit<AppFactoryParams, 'appSpec'>) {
    this.appFactory = new _AppFactory({
      ...params,
      appSpec: APP_SPEC,
    })
  }
  
  /** The name of the app (from the ARC-32 / ARC-56 app spec or override). */
  public get appName() {
    return this.appFactory.appName
  }
  
  /** The ARC-56 app spec being used */
  get appSpec() {
    return APP_SPEC
  }
  
  /** A reference to the underlying `AlgorandClient` this app factory is using. */
  public get algorand(): AlgorandClient {
    return this.appFactory.algorand
  }
  
  /**
   * Returns a new `AppClient` client for an app instance of the given ID.
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public getAppClientById(params: AppFactoryAppClientParams) {
    return new PassportRegistryClient(this.appFactory.getAppClientById(params))
  }
  
  /**
   * Returns a new `AppClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   *
   * Automatically populates appName, defaultSender and source maps from the factory
   * if not specified in the params.
   * @param params The parameters to create the app client
   * @returns The `AppClient`
   */
  public async getAppClientByCreatorAndName(
    params: AppFactoryResolveAppClientByCreatorAndNameParams,
  ) {
    return new PassportRegistryClient(await this.appFactory.getAppClientByCreatorAndName(params))
  }

  /**
   * Idempotently deploys the PassportRegistry smart contract.
   *
   * @param params The arguments for the contract calls and any additional parameters for the call
   * @returns The deployment result
   */
  public async deploy(params: PassportRegistryDeployParams = {}) {
    const result = await this.appFactory.deploy({
      ...params,
    })
    return { result: result.result, appClient: new PassportRegistryClient(result.appClient) }
  }

  /**
   * Get parameters to create transactions (create and deploy related calls) for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the PassportRegistry smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The params for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.params.bare.create(params)
      },
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the PassportRegistry smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The transaction for a create call
       */
      bare: (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        return this.appFactory.createTransaction.bare.create(params)
      },
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Gets available create methods
     */
    create: {
      /**
       * Creates a new instance of the PassportRegistry smart contract using a bare call.
       *
       * @param params The params for the bare (raw) call
       * @returns The create result
       */
      bare: async (params?: Expand<AppClientBareCallParams & AppClientCompilationParams & CreateSchema & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}>) => {
        const result = await this.appFactory.send.bare.create(params)
        return { result: result.result, appClient: new PassportRegistryClient(result.appClient) }
      },
    },

  }

}
/**
 * A client to make calls to the PassportRegistry smart contract
 */
export class PassportRegistryClient {
  /**
   * The underlying `AppClient` for when you want to have more flexibility
   */
  public readonly appClient: _AppClient

  /**
   * Creates a new instance of `PassportRegistryClient`
   *
   * @param appClient An `AppClient` instance which has been created with the PassportRegistry app spec
   */
  constructor(appClient: _AppClient)
  /**
   * Creates a new instance of `PassportRegistryClient`
   *
   * @param params The parameters to initialise the app client with
   */
  constructor(params: Omit<AppClientParams, 'appSpec'>)
  constructor(appClientOrParams: _AppClient | Omit<AppClientParams, 'appSpec'>) {
    this.appClient = appClientOrParams instanceof _AppClient ? appClientOrParams : new _AppClient({
      ...appClientOrParams,
      appSpec: APP_SPEC,
    })
  }
  
  /**
   * Checks for decode errors on the given return value and maps the return value to the return type for the given method
   * @returns The typed return value or undefined if there was no value
   */
  decodeReturnValue<TSignature extends PassportRegistryNonVoidMethodSignatures>(method: TSignature, returnValue: ABIReturn | undefined) {
    return returnValue !== undefined ? getArc56ReturnValue<MethodReturn<TSignature>>(returnValue, this.appClient.getABIMethod(method), APP_SPEC.structs) : undefined
  }
  
  /**
   * Returns a new `PassportRegistryClient` client, resolving the app by creator address and name
   * using AlgoKit app deployment semantics (i.e. looking for the app creation transaction note).
   * @param params The parameters to create the app client
   */
  public static async fromCreatorAndName(params: Omit<ResolveAppClientByCreatorAndName, 'appSpec'>): Promise<PassportRegistryClient> {
    return new PassportRegistryClient(await _AppClient.fromCreatorAndName({...params, appSpec: APP_SPEC}))
  }
  
  /**
   * Returns an `PassportRegistryClient` instance for the current network based on
   * pre-determined network-specific app IDs specified in the ARC-56 app spec.
   *
   * If no IDs are in the app spec or the network isn't recognised, an error is thrown.
   * @param params The parameters to create the app client
   */
  static async fromNetwork(
    params: Omit<ResolveAppClientByNetwork, 'appSpec'>
  ): Promise<PassportRegistryClient> {
    return new PassportRegistryClient(await _AppClient.fromNetwork({...params, appSpec: APP_SPEC}))
  }
  
  /** The ID of the app instance this client is linked to. */
  public get appId() {
    return this.appClient.appId
  }
  
  /** The app address of the app instance this client is linked to. */
  public get appAddress() {
    return this.appClient.appAddress
  }
  
  /** The name of the app. */
  public get appName() {
    return this.appClient.appName
  }
  
  /** The ARC-56 app spec being used */
  public get appSpec() {
    return this.appClient.appSpec
  }
  
  /** A reference to the underlying `AlgorandClient` this app client is using. */
  public get algorand(): AlgorandClient {
    return this.appClient.algorand
  }

  /**
   * Get parameters to create transactions for the current app. A good mental model for this is that these parameters represent a deferred transaction creation.
   */
  readonly params = {
    /**
     * Makes a clear_state call to an existing instance of the PassportRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.params.bare.clearState(params)
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `register_passport(string,byte[])void` ABI method.
     *
     * Register a passport hash on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    registerPassport: (params: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PassportRegistryParamsFactory.registerPassport(params))
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `verify_passport(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the stored hash for a passport (read-only).
     *
     * @param params The params for the smart contract call
     * @returns The call params: The SHA-256 hash stored on-chain
     */
    verifyPassport: (params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PassportRegistryParamsFactory.verifyPassport(params))
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `passport_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a passport is registered.
     *
     * @param params The params for the smart contract call
     * @returns The call params
     */
    passportExists: (params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.params.call(PassportRegistryParamsFactory.passportExists(params))
    },

  }

  /**
   * Create transactions for the current app
   */
  readonly createTransaction = {
    /**
     * Makes a clear_state call to an existing instance of the PassportRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams>) => {
      return this.appClient.createTransaction.bare.clearState(params)
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `register_passport(string,byte[])void` ABI method.
     *
     * Register a passport hash on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    registerPassport: (params: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PassportRegistryParamsFactory.registerPassport(params))
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `verify_passport(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the stored hash for a passport (read-only).
     *
     * @param params The params for the smart contract call
     * @returns The call transaction: The SHA-256 hash stored on-chain
     */
    verifyPassport: (params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PassportRegistryParamsFactory.verifyPassport(params))
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `passport_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a passport is registered.
     *
     * @param params The params for the smart contract call
     * @returns The call transaction
     */
    passportExists: (params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      return this.appClient.createTransaction.call(PassportRegistryParamsFactory.passportExists(params))
    },

  }

  /**
   * Send calls to the current app
   */
  readonly send = {
    /**
     * Makes a clear_state call to an existing instance of the PassportRegistry smart contract.
     *
     * @param params The params for the bare (raw) call
     * @returns The clearState result
     */
    clearState: (params?: Expand<AppClientBareCallParams & SendParams>) => {
      return this.appClient.send.bare.clearState(params)
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `register_passport(string,byte[])void` ABI method.
     *
     * Register a passport hash on-chain.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    registerPassport: async (params: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PassportRegistryParamsFactory.registerPassport(params))
      return {...result, return: result.return as unknown as (undefined | PassportRegistryReturns['register_passport(string,byte[])void'])}
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `verify_passport(string)byte[]` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Retrieve the stored hash for a passport (read-only).
     *
     * @param params The params for the smart contract call
     * @returns The call result: The SHA-256 hash stored on-chain
     */
    verifyPassport: async (params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PassportRegistryParamsFactory.verifyPassport(params))
      return {...result, return: result.return as unknown as (undefined | PassportRegistryReturns['verify_passport(string)byte[]'])}
    },

    /**
     * Makes a call to the PassportRegistry smart contract using the `passport_exists(string)bool` ABI method.
     * 
     * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
     *
     * Check if a passport is registered.
     *
     * @param params The params for the smart contract call
     * @returns The call result
     */
    passportExists: async (params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']> & SendParams & {onComplete?: OnApplicationComplete.NoOpOC}) => {
      const result = await this.appClient.send.call(PassportRegistryParamsFactory.passportExists(params))
      return {...result, return: result.return as unknown as (undefined | PassportRegistryReturns['passport_exists(string)bool'])}
    },

  }

  /**
   * Clone this app client with different params
   *
   * @param params The params to use for the the cloned app client. Omit a param to keep the original value. Set a param to override the original value. Setting to undefined will clear the original value.
   * @returns A new app client with the altered params
   */
  public clone(params: CloneAppClientParams) {
    return new PassportRegistryClient(this.appClient.clone(params))
  }

  /**
   * Makes a readonly (simulated) call to the PassportRegistry smart contract using the `verify_passport(string)byte[]` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Retrieve the stored hash for a passport (read-only).
   *
   * @param params The params for the smart contract call
   * @returns The call result: The SHA-256 hash stored on-chain
   */
  async verifyPassport(params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']>) {
    const result = await this.appClient.send.call(PassportRegistryParamsFactory.verifyPassport(params))
    return result.return as unknown as PassportRegistryReturns['verify_passport(string)byte[]']
  }

  /**
   * Makes a readonly (simulated) call to the PassportRegistry smart contract using the `passport_exists(string)bool` ABI method.
   * 
   * This method is a readonly method; calling it with onComplete of NoOp will result in a simulated transaction rather than a real transaction.
   *
   * Check if a passport is registered.
   *
   * @param params The params for the smart contract call
   * @returns The call result
   */
  async passportExists(params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']>) {
    const result = await this.appClient.send.call(PassportRegistryParamsFactory.passportExists(params))
    return result.return as unknown as PassportRegistryReturns['passport_exists(string)bool']
  }

  /**
   * Methods to access state for the current PassportRegistry app
   */
  state = {
    /**
     * Methods to access box state for the current PassportRegistry app
     */
    box: {
      /**
       * Get all current keyed values from box state
       */
      getAll: async (): Promise<Partial<Expand<BoxKeysState>>> => {
        const result = await this.appClient.state.box.getAll()
        return {
        }
      },
      /**
       * Get values from the passport_hashes map in box state
       */
      passportHashes: {
        /**
         * Get all current values of the passport_hashes map in box state
         */
        getMap: async (): Promise<Map<string, Uint8Array>> => { return (await this.appClient.state.box.getMap("passport_hashes")) as Map<string, Uint8Array> },
        /**
         * Get a current value of the passport_hashes map by key from box state
         */
        value: async (key: string): Promise<Uint8Array | undefined> => { return await this.appClient.state.box.getMapValue("passport_hashes", key) as Uint8Array | undefined },
      },
    },
  }

  public newGroup(): PassportRegistryComposer {
    const client = this
    const composer = this.algorand.newGroup()
    let promiseChain:Promise<unknown> = Promise.resolve()
    const resultMappers: Array<undefined | ((x: ABIReturn | undefined) => any)> = []
    return {
      /**
       * Add a register_passport(string,byte[])void method call against the PassportRegistry contract
       */
      registerPassport(params: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.registerPassport(params)))
        resultMappers.push(undefined)
        return this
      },
      /**
       * Add a verify_passport(string)byte[] method call against the PassportRegistry contract
       */
      verifyPassport(params: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.verifyPassport(params)))
        resultMappers.push((v) => client.decodeReturnValue('verify_passport(string)byte[]', v))
        return this
      },
      /**
       * Add a passport_exists(string)bool method call against the PassportRegistry contract
       */
      passportExists(params: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']> & {onComplete?: OnApplicationComplete.NoOpOC}) {
        promiseChain = promiseChain.then(async () => composer.addAppCallMethodCall(await client.params.passportExists(params)))
        resultMappers.push((v) => client.decodeReturnValue('passport_exists(string)bool', v))
        return this
      },
      /**
       * Add a clear state call to the PassportRegistry contract
       */
      clearState(params: AppClientBareCallParams) {
        promiseChain = promiseChain.then(() => composer.addAppCall(client.params.clearState(params)))
        return this
      },
      addTransaction(txn: Transaction, signer?: TransactionSigner) {
        promiseChain = promiseChain.then(() => composer.addTransaction(txn, signer))
        return this
      },
      async composer() {
        await promiseChain
        return composer
      },
      async simulate(options?: SimulateOptions) {
        await promiseChain
        const result = await (!options ? composer.simulate() : composer.simulate(options))
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      },
      async send(params?: SendParams) {
        await promiseChain
        const result = await composer.send(params)
        return {
          ...result,
          returns: result.returns?.map((val, i) => resultMappers[i] !== undefined ? resultMappers[i]!(val) : val.returnValue)
        }
      }
    } as unknown as PassportRegistryComposer
  }
}
export type PassportRegistryComposer<TReturns extends [...any[]] = []> = {
  /**
   * Calls the register_passport(string,byte[])void ABI method.
   *
   * Register a passport hash on-chain.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  registerPassport(params?: CallParams<PassportRegistryArgs['obj']['register_passport(string,byte[])void'] | PassportRegistryArgs['tuple']['register_passport(string,byte[])void']>): PassportRegistryComposer<[...TReturns, PassportRegistryReturns['register_passport(string,byte[])void'] | undefined]>

  /**
   * Calls the verify_passport(string)byte[] ABI method.
   *
   * Retrieve the stored hash for a passport (read-only).
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  verifyPassport(params?: CallParams<PassportRegistryArgs['obj']['verify_passport(string)byte[]'] | PassportRegistryArgs['tuple']['verify_passport(string)byte[]']>): PassportRegistryComposer<[...TReturns, PassportRegistryReturns['verify_passport(string)byte[]'] | undefined]>

  /**
   * Calls the passport_exists(string)bool ABI method.
   *
   * Check if a passport is registered.
   *
   * @param args The arguments for the contract call
   * @param params Any additional parameters for the call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  passportExists(params?: CallParams<PassportRegistryArgs['obj']['passport_exists(string)bool'] | PassportRegistryArgs['tuple']['passport_exists(string)bool']>): PassportRegistryComposer<[...TReturns, PassportRegistryReturns['passport_exists(string)bool'] | undefined]>

  /**
   * Makes a clear_state call to an existing instance of the PassportRegistry smart contract.
   *
   * @param args The arguments for the bare call
   * @returns The typed transaction composer so you can fluently chain multiple calls or call execute to execute all queued up transactions
   */
  clearState(params?: AppClientBareCallParams): PassportRegistryComposer<[...TReturns, undefined]>

  /**
   * Adds a transaction to the composer
   *
   * @param txn A transaction to add to the transaction group
   * @param signer The optional signer to use when signing this transaction.
   */
  addTransaction(txn: Transaction, signer?: TransactionSigner): PassportRegistryComposer<TReturns>
  /**
   * Returns the underlying AtomicTransactionComposer instance
   */
  composer(): Promise<TransactionComposer>
  /**
   * Simulates the transaction group and returns the result
   */
  simulate(): Promise<PassportRegistryComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: SkipSignaturesSimulateOptions): Promise<PassportRegistryComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  simulate(options: RawSimulateOptions): Promise<PassportRegistryComposerResults<TReturns> & { simulateResponse: SimulateResponse }>
  /**
   * Sends the transaction group to the network and returns the results
   */
  send(params?: SendParams): Promise<PassportRegistryComposerResults<TReturns>>
}
export type PassportRegistryComposerResults<TReturns extends [...any[]]> = Expand<SendAtomicTransactionComposerResults & {
  returns: TReturns
}>

